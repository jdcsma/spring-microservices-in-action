#
#   This is the core configuration for service.  There are two examples here:
#
#       -  A configuration service which completely loads the configuration for the services
#          from the local file server of the config service. This should only be used for
#          demonstration purposes or for a small application with a limited number of services.
#
#       -  A configuration service that uses a git-based repository to read the files from.
#

####
# Classpath and file-based solution
####

spring:
  application:
    name: spring-cloud-config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        encrypt.enabled: false
        native:
          # searchLocations: file://<ch4>/spring-cloud-service-discovery/spring-cloud-config-server/src/main/resources/config/licensing-service,
          #                  file:/<ch4>/spring-cloud-service-discovery/spring-cloud-config-server/src/main/resources/config/organization-service
          searchLocations: classpath:config/licensing-service,classpath:config/organization-service

####
# Git-based solution
####

#spring:
#  application:
#    name: spring-cloud-config-server
#  profiles:
#    active: native
#  cloud:
#    config:
#      server:
#        encrypt.enabled: false
#        git:
#          uri: https://github.com/xxxx/config-repo/
#          searchPaths: licensing-service,organization-service
#          username: username
#          password: password

# 环境变量: export ENCRYPT_KEY=IMSYMMETRIC
encrypt:
  key: IMSYMMETRIC
