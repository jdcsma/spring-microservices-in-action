spring:
  application:
    name: licensing-service
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8800
# 环境变量: export ENCRYPT_KEY=IMSYMMETRIC
#encrypt:
#  key: IMSYMMETRIC
